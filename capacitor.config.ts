import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'goearns-global-learning',
  webDir: 'dist'
};

export default config;
